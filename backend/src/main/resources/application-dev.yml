spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/analytics_platform
    username: analytics_user
    password: analytics_password
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    com.analytics.platform: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
